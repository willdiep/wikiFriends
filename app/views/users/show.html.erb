

<br />


<h1>
    Username: <%= @user.username %>
</h1>

<h2> 
    User's Name: <%= @user.first_name %> <%= @user.last_name %>
</h2>

 

<% @user.saved_articles.each do |sa| %>
    <p>
        <%= link_to sa.article.title, sa.article %>: <%= sa.article.short_desc %>
    </p>

    <p>
        Note: <%= sa.note %>
    </p>

    Tags:
    <% sa.article.tags.each do |t| %>
        <%= t.name %>
    <% end %>

    <br />
<% end %>